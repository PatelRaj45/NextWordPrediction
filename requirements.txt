# requirements.txt  (runtime only)
streamlit==1.35.0
tensorflow==2.16.1          # or tensorflow-cpu==2.16.1  (smaller, prefers CPU)
numpy>=1.23,<2.0
h5py>=3.10,<4.0             # implicit dep, but explicit pin avoids surprises
